apiVersion: v1
kind: Service
metadata:
  name: moscow-time-app
  labels:
    app: moscow-time-app
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: moscow-time-app 